[general]
name = "Button and Encoder"
description = "Обрабатывает нажатия кнопки (одиночный клик, двойной клик, долгое нажатие) и вращение энкодера. Публикует события в топики `button_click/pepeunit`, `encoder_rotation/pepeunit` и опциональные сообщения в `*_message/pepeunit`"
language = "Micropython"
hardware = ["esp8266", "KY-040", "button"]
firmware = [
  {name = "ESP8266_GENERIC-v1.27.0-PEPEUNIT-v1.1.1.bin", link = "https://git.pepemoss.com/pepe/pepeunit/libs/pepeunit_micropython_client/-/package_files/60/download"}
]
stack = ["pepeunit_micropython_client"]
version = "1.1.1"
license = "AGPL v3 License"
authors = [
    {name = "Ivan Serebrennikov", email = "admin@silberworks.com"}
]


[images]
schema = "https://minio.pepemoss.com/public-data/image/button_encoder.png"

[physical_io]
"client.settings.PIN_BUTTON" = "Вывод кнопки"
"client.settings.PIN_ENCODER_CLK" = "Вывод `CLK` энкодера (работет только при `FF_ENCODER_ENABLE` = `true`)"
"client.settings.PIN_ENCODER_DT" = "Вывод `DT` энкодера (работет только при `FF_ENCODER_ENABLE` = `true`)"

[env_description]
FF_ENCODER_ENABLE = "Включить энкодер: `true` или `false`"
PIN_BUTTON = "Номер пина кнопки"
PIN_ENCODER_CLK = "Номер пина `CLK` энкодера"
PIN_ENCODER_DT = "Номер пина `DT` энкодера"
ONE_CLICK_MESSAGE = "Сообщение при одиночном клике (или `null` — не публиковать)"
DOUBLE_CLICK_MESSAGE = "Сообщение при двойном клике (или `null` — не публиковать)"
LONG_PRESS_MESSAGE = "Сообщение при долгом нажатии (или `null` — не публиковать)"
RIGHT_ROTATE_MESSAGE = "Сообщение при повороте вправо (или `null` — не публиковать)"
LEFT_ROTATE_MESSAGE = "Сообщение при повороте влево (или `null` — не публиковать)"
BUTTON_DEBOUNCE_TIME = "Время антидребезга кнопки в миллисекундах"
BUTTON_DOUBLE_CLICK_TIME = "Окно для двойного клика в миллисекундах"
BUTTON_LONG_PRESS_TIME = "Время долгого нажатия в миллисекундах"
ENCODER_DEBOUNCE_TIME = "Время антидребезга энкодера в миллисекундах"
PUC_WIFI_SSID = "Имя сети `WiFi`"
PUC_WIFI_PASS = "Пароль от сети `WiFi`"

[topic_assignment]
"button_click/pepeunit" = "Тип нажатия кнопки: `One`, `Double` или `Long`"
"button_one_click_message/pepeunit" = "Публикует `ONE_CLICK_MESSAGE` при одиночном клике (если задано)"
"button_double_click_message/pepeunit" = "Публикует `DOUBLE_CLICK_MESSAGE` при двойном клике (если задано)"
"button_long_press_message/pepeunit" = "Публикует `LONG_PRESS_MESSAGE` при долгом нажатии (если задано)"
"encoder_rotation/pepeunit" = "Направление вращения энкодера: `Right` или `Left`"
"encoder_right_rotate_message/pepeunit" = "Публикует `RIGHT_ROTATE_MESSAGE` при повороте вправо (если задано)"
"encoder_left_rotate_message/pepeunit" = "Публикует `LEFT_ROTATE_MESSAGE` при повороте влево (если задано)"

[work_algorithm]
steps = [
  "Подключение к `WiFi`",
  "Подключение к `MQTT` Брокеру",
  "Синхронизация времени по `NTP`",
  "Инициализация пина кнопки и пинов энкодера (если `FF_ENCODER_ENABLE` = `true`)",
  "Запуск цикла опроса кнопки и энкодера каждую миллисекунду",
  "При нажатии кнопки: публикация типа в `button_click/pepeunit` (`One`, `Double`, `Long`)",
  "При наличии `*_CLICK_MESSAGE` — дополнительная публикация в соответствующий топик `*_message/pepeunit`",
  "При вращении энкодера: публикация направления в `encoder_rotation/pepeunit` (`Right`, `Left`)",
  "При наличии `*_ROTATE_MESSAGE` — дополнительная публикация в соответствующий топик `*_message/pepeunit`"
]

[installation]
steps = [
  "Установите образ `Micropython` указанный в `firmware` на `esp8266`, как это сделано в [руководстве](https://micropython.org/download/ESP8266_GENERIC/)",
  "Создайте `Unit` в `Pepeunit`",
  "Установите переменные окружения в `Pepeunit`",
  "Скачайте архив c программой из `Pepeunit`",
  "Распакуйте архив в директорию",
  "Загрузите файлы из директории на физическое устройство, например командой: `ampy -p /dev/ttyUSB0 -b 115200 put ./ .`",
  "Запустить устройство нажатием кнопки `reset`"
]
